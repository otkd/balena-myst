version: "2.1"
volumes:
  myst-data:
dns:
  - 1.1.1.1
  - 8.8.8.8
services:
  myst:
    build: ./myst
    privileged: true
    restart: always
    network_mode: host
    volumes:
      - "myst-data:/var/lib/mysterium-node"
    labels:
      io.balena.features.kernel-modules: '1'
